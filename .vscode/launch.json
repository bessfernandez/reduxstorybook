{
  "configurations": [
    {
        "name": "Chrome debugger (requires plugin)",
        "type": "chrome",
        "request": "launch",
        "url": "http://localhost:3000",
        "webRoot": "${workspaceRoot}"
    }
  ]
}